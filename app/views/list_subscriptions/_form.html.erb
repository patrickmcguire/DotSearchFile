<%= form_for(@list_subscription) do |f| %>
  <% if @list_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_subscription.errors.count, "error") %> prohibited this list_subscription from being saved:</h2>

      <ul>
      <% @list_subscription.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
